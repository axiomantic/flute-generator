// Page entry point. index.html loads only this bundle; the studio runtime registers its
// DOM listeners and schedules boot as a side effect of being imported, exactly as the
// former inline <script> did.
import './ui/app.js';
